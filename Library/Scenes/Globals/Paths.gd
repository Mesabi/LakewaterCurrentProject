extends Node

#to exist as a dictionary for various paths. 
#this will be hard coded, but I'll add some features to do custom as well. 
#NOTE Technically this doesn't need to be a dictionary. (if in same folder, it's fine...)
#actually I should do this as an array, keeping both for sanity.
var levelDictionary = {
	
	"error" : "PATH"
	
	
}
var levels = [
	
	"Error",
	"GD4Test-01"
	
	
]
